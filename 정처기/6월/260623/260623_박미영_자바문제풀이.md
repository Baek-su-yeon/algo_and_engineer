### 단순 while문

- 특정 조건이 참인 동안 계속해서 코드 블록을 실행하게 함.
- 조건이 거짓이 되는 순간, 반복이 멈춤.

```java
int i = 0;  // 'i'라는 변수를 0으로 시작함.
while (i < 5) {  // 'i'가 5보다 작은 동안 계속 반복
		System.out.println("i의 값은 " + i + "입니다.");  // 현재 'i'의 값을 출력
		i++;  // 그리고 'i'를 1씩 증가시킴.
}

```

- **for문과의 차이**
    - while 문은 조건이 참인 동안 계속 반복되는데, 주로 반복 횟수가 명확하지 않을 때 사용
    - for 문은 반복 횟수가 정해져 있거나, 반복을 위한 변수의 시작과 끝이 명확할 때 주로 사용됨.

- **continue**
    - continue를 만나면, 반복문의 현재 반복을 즉시 종료하고 다음 반복으로 넘어감.
    - 반복문의 나머지 부분을 건너뛰고, 반복문의 시작 부분으로 돌아가 새로운 반복을 시작함.
- **break**
    - 반복문을 완전히 끝내는 역할
    - break를 만나면, 그 즉시 반복문이 종료되고 반복문 바깥의 다음 코드로 넘어감.

### [기출 예제] 20년 3회 실기

- **다음은 자바 코드이다. 출력 결과를 쓰시오.**

```java
public class Main {
		public static void main(String[] args) {
				int i = 0;
				int sum = 0;
				while (i < 10) {
						i++;
						if (i % 2 == 1)
								continue;
						sum += i;
				}
				System.out.println(sum);
		}
}
```

- 정답
    
    30

### 상속

- 부모 클래스가 가진 특성(변수)과 행동(메서드)을 자식 클래스가 물려받는 것

```java
class Parent {
		void sayHello() {
				System.out.println("Hello, I'm a parent!");
		}
}

class Child extends Parent {
		// Child 클래스는 Parent 클래스의 sayHello 메서드를 상속받음.
}
```

- Child 클래스는 Parent 클래스로부터 sayHello 메서드를 상속받으니, Child 객체를 만들어 sayHello를 호출하면 “Hello, I’m a parent!” 라는 메시지가 출력됨.

### 오버라이딩

- 상속받은 메서들르 자식 클래스에서 재정의하는 것
- 자식 클래스가 상속받은 메서드를 마음에 들게 바꾸고 싶을 때 사용

```java
class Child extends Parent {
		void sayHello() {
				System.out.println("Hello, I'm a child!");
		}
}
```

- Child 클래스는 Parent의 sayHello 메서드를 오버라이딩해서 “Hello, I’m a chlid!” 라는 새 메시지를 출력하도록 변경

### 오버로딩

- 같은 이름의 메서드를 여러 개 가지되, 매개변수의 유형이나 개수를 다르게 하는 것
- 같은 일을 하는 메서드라도 조금씩 다른 상황에서 사용할 수 있음.

```java
class MathOperations {
		int sum(int a, int b) {
				return a + b;
		}
		
		// 오버로딩된 sum 메서드, 매개변수의 개수가 다름.
		int sum(int a, int b, int c) {
				return a + b + c;
		}
}
```

- MathOperations 클래스에는 sum이라는 메서드가 두 개 있음.
- 하나는 두 숫자를 더하고, 다른 하나는 세 숫자를 더함.
- 매개변수가 다르기 때문에 두 메서드는 서로 다른 버전으로 존재할 수 있음.

### [기출 예제] 23년 3회 실기

- **다음은 Java 코드이다. 올바른 출력 결과를 작성하시오.**

```java
public class main{
		public static void main(String[] args) {
				A b = new B();
				b.paint();
				b.draw();
		}
}

class A {
		public void paint() {
				System.out.print("A");
				draw();
		}
		
		public void draw() {
				System.out.print("B");
				draw();
		}
}

class B extends A {
		public void paint() {
				super.draw();
				System.out.print("C");
				this.draw();
		}
		
		public void draw() {
				System.out.print("D");
		}
}
		
```

- 정답
    
    BDCDD
    
- 풀이
    
    ```java
    // 실제 실행코드
    public class main{
    		public static void main(String[] args) {
    				A b = new B();  // 1. 자식 클래스 B의 객체 생성
    				b.paint();  // 2. 자식 클래스 B의 paint() 실행 
    				b.draw();  // 10. 자식 클래스 B의 draw() 실행
    		}
    }
    
    // 부모 클래스 A
    class A {
    		public void paint() {
    				System.out.print("A");
    				draw();
    		}
    		
    		public void draw() {
    				System.out.print("B");  // 4. 'B' 출력
    				draw();  // 5. 자식 클래스 draw)) 실행
    		}
    }
    
    // 자식 클래스 B
    class B extends A {
    		public void paint() {
    				super.draw();  // 3. 부모 클래스 A의 draw() 호출
    				System.out.print("C");  // 7. 'C' 출력
    				this.draw();  // 8. 자식 클래스 B의 draw() 호출
    		}
    		
    		public void draw() {
    				System.out.print("D");  // 6, 9, 11. 'D' 출력
    		}
    }
    		
    ```
    
    - super.~ : 부모의 메소드를 호출
    - this.~ : 현재 실행 중인 나의 메소드 호출 / 생략도 가능
